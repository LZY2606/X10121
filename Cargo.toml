[package]
name = "frame-parse-lab"
version = "0.1.0"
edition = "2021"
description = "帧解析实验室: browser-based protocol frame parsing lab"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tiny_http = "0.12"

[profile.release]
opt-level = 2
